[build-system]

requires = [
    "hatchling"
]
build-backend = "hatchling.build"

[project]
name = "modelctl-sdk"
version = "0.3.0"
description = "SDK for modelctl plugins"
requires-python = ">=3.13"

[tool.hatch.build.targets.wheel]
packages = [
    "src/modelctl_sdk"
]
