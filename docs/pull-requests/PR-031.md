# PR #31 — Declare v0.2.0 ready and promote to main

- Pull request: https://github.com/LEEBONGHAK/modelctl/pull/31
- Status: Open (draft)
- Base / head: `main` ← `release/v0.2.0-readiness`
- Readiness base commit: `1fb85812b5aca2232ac2fb479e35f38d837bb229`

## English

### Context

PRs #21–#30 completed the v0.2.0 functional, quality, compatibility, provider, CI, packaging, and dependency-security scope on `refac`. The final preparation commit includes `cryptography 50.0.0`, a clean dependency audit, 137 cross-platform tests, successful package builds, installed-wheel smoke tests, and a complete release dry-run.

This pull request contains no new runtime feature. It declares the validated version ready and promotes the exact development lineage to the canonical `main` release branch.

### Changes

- Changed `release.toml` from `draft` to `ready` while keeping PyPI disabled.
- Dated and finalized the v0.2.0 changelog entry.
- Marked all v0.2.0 functional, quality, security, and release criteria satisfied.
- Updated English and Korean READMEs for the completed v0.2.0 workflows.
- Removed the obsolete temporary dependency-exception text from the security policy.
- Corrected release trust documentation so tags must belong to `main`.
- Documented the trusted merged-PR path and owner-only fallback.
- Finalized PR #30 and added this bilingual readiness record.

### Required validation

The pull-request dry run must pass:

- release metadata, package version, documentation, and `v0.2.0` tag validation
- locked dependency installation and audit without exclusions
- Ruff and native-provider contract tests
- all 137 tests on Ubuntu, macOS, and Windows with Python 3.13
- all wheel and source-distribution builds
- isolated installed-wheel package and CLI smoke tests
- SHA-256 checksum generation

### Publication outcome

After merge, the release workflow must check out the exact `main` merge commit and independently repeat every release gate. It may then create immutable tag `v0.2.0` and one GitHub Release with six Python distributions and `SHA256SUMS`.

PyPI publication remains disabled. If the normal merged-event publication is not emitted, the repository owner may invoke the existing validated `/release v0.2.0` fallback on this merged pull request.

## 한국어

### 배경

PR #21–#30에서 `refac`의 v0.2.0 기능, 품질, 호환성, provider, CI, packaging, dependency 보안 범위를 완료했습니다. 최종 준비 commit에는 `cryptography 50.0.0`, clean dependency audit, 137개 cross-platform 테스트, package build, 설치 wheel smoke test, 전체 release dry-run 결과가 포함됩니다.

이 Pull Request는 새로운 runtime 기능을 추가하지 않습니다. 검증된 버전을 ready로 선언하고 정확한 개발 계보를 공식 release branch인 `main`으로 승격합니다.

### 변경 내용

- PyPI 비활성화를 유지하면서 `release.toml`을 `draft`에서 `ready`로 변경했습니다.
- v0.2.0 changelog에 날짜를 지정하고 완료했습니다.
- v0.2.0 기능·품질·보안·릴리스 기준 충족을 기록했습니다.
- 영문·한국어 README를 완성된 v0.2.0 흐름에 맞게 갱신했습니다.
- Security policy에서 오래된 임시 dependency 예외 문구를 제거했습니다.
- Release tag가 `main`에 포함된 commit을 가리키도록 신뢰 문서를 수정했습니다.
- 신뢰된 병합 PR 경로와 owner-only fallback을 문서화했습니다.
- PR #30 이력을 완료하고 이번 readiness 이력을 추가했습니다.

### 필수 검증

Pull Request dry-run에서 다음 항목이 통과해야 합니다.

- Release metadata, package version, 문서, `v0.2.0` tag 검증
- 예외 없는 locked dependency 설치·audit
- Ruff와 native provider contract 테스트
- Python 3.13 기반 Ubuntu·macOS·Windows 전체 137개 테스트
- 모든 wheel·source distribution build
- 격리된 wheel 설치와 package·CLI smoke test
- SHA-256 checksum 생성

### 게시 결과

병합 후 release workflow는 정확한 `main` merge commit에서 모든 release gate를 독립적으로 다시 실행해야 합니다. 이후 불변 tag `v0.2.0`과 Python 배포물 여섯 개 및 `SHA256SUMS`를 포함한 하나의 GitHub Release를 생성할 수 있습니다.

PyPI 게시는 계속 비활성화합니다. 정상 병합 이벤트가 게시 workflow를 생성하지 않으면 저장소 owner가 이 병합 PR에 기존 검증된 `/release v0.2.0` fallback을 실행할 수 있습니다.
