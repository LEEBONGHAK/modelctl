# PR #38 — Superseded launcher plugin discovery draft

- Pull request: https://github.com/LEEBONGHAK/modelctl/pull/38
- Base branch: `refac`
- Head branch: `feat/v0.3.0-launcher-plugin-discovery`
- Created: 2026-09-01
- Status: Closed unmerged
- Merge commit: None
- Superseded by: PR #39

## English

### Outcome

The installed launcher plugin discovery implementation and all validation completed on head `47558815e1fd5a67d327674193dcc425ded27408`. However, the connected GitHub draft-to-ready mutation failed because its GraphQL response requested a field no longer exposed by GitHub. GitHub correctly refused to merge a draft PR.

PR #38 was therefore closed unmerged without changing or reverting the code. PR #39 was opened as a non-draft PR from the exact same branch and implementation lineage.

### Validation before closure

- Clean locked dependency audit and Ruff passed.
- Provider contract suites passed.
- Complete suite reached 163 passing tests after fixing output-only test assumptions.
- Ubuntu, macOS, and Windows passed with Python 3.13.
- Distribution builds and isolated wheel installation passed.
- An installed fixture package was discovered through `modelctl.launchers` with source `modelctl-test-launcher-plugin==0.1.0`.
- Release metadata dry-run passed with v0.3.0 remaining `draft`.

No feature work was discarded; implementation details are recorded under PR #39.

## 한국어

### 결과

설치형 launcher plugin discovery 구현과 모든 검증은 head `47558815e1fd5a67d327674193dcc425ded27408`에서 완료됐습니다. 다만 연결된 GitHub의 Draft → Ready GraphQL mutation이 GitHub에서 더 이상 제공하지 않는 응답 필드를 요청하면서 실패했고, GitHub는 Draft PR 병합을 정상적으로 거부했습니다.

따라서 PR #38은 코드 변경이나 revert 없이 미병합 종료했습니다. 동일한 branch와 구현 계보에서 non-draft PR #39를 생성했습니다.

### 종료 전 검증

- 잠긴 dependency audit과 Ruff가 통과했습니다.
- Provider contract suite가 통과했습니다.
- 출력 형식만 가정하던 테스트를 수정한 뒤 전체 163개 테스트가 통과했습니다.
- Python 3.13 기반 Ubuntu, macOS, Windows가 통과했습니다.
- Distribution build와 격리 wheel 설치가 통과했습니다.
- 설치된 fixture package를 `modelctl.launchers`에서 탐색했고 source `modelctl-test-launcher-plugin==0.1.0`을 확인했습니다.
- v0.3.0을 `draft`로 유지한 release metadata dry-run이 통과했습니다.

기능 구현은 폐기되지 않았으며 실제 구현 내역은 PR #39에 기록합니다.
