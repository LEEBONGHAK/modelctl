# PR #42 — v0.3.0 final documentation and release-criteria review

- Pull request: pending
- Base branch: `refac`
- Head branch: `feat/v0.3.0-final-docs`
- Created: 2026-09-01
- Status: validation pending

## English

### Context

The functional v0.3.0 increments are complete: named profiles, the public launcher-plugin SDK contract, installed launcher discovery, plugin diagnostics/compatibility hardening, and strict boundary type checking. This pull request performs the final documentation and scope review before readiness validation and promotion to `main`.

### Review decisions

- Profile portability is not required for v0.3.0 because no concrete usage need has been demonstrated; it remains deferred rather than an incomplete release blocker.
- v0.3.0 contains no provider plugins, automatic plugin installation, remote plugin registry, arbitrary filesystem plugin loading, credential export, or PyPI publication.
- `release.toml` remains `draft` in this documentation PR. Only the dedicated readiness PR may change it to `ready`.
- The final readiness path is: clean validation of the completed `refac` tree, then one dedicated `main`-targeting readiness PR that changes release metadata and promotes the exact validated lineage.

### Documentation updates

- Reconcile English/Korean READMEs with the completed v0.3.0 feature set and remaining release-only work.
- Mark final documentation review complete in release criteria while keeping final validation/readiness items pending.
- Update project progress from active feature development to readiness preparation.
- Record this PR in the bilingual engineering history.

## 한국어

### 배경

이름 있는 profile, 공개 launcher-plugin SDK 계약, 설치 launcher 탐색, plugin 진단/호환성 강화, strict boundary type check까지 v0.3.0 기능 increment가 모두 완료되었습니다. 이번 Pull Request는 readiness validation과 `main` 승격 전에 최종 문서 및 scope 검토를 수행합니다.

### 검토 결정

- Profile portability는 실제 사용에서 구체적인 필요가 확인되지 않았으므로 v0.3.0 필수 범위가 아닙니다. 미완료 blocker가 아니라 후속 버전으로 defer합니다.
- v0.3.0에는 provider plugin, plugin 자동 설치, remote plugin registry, 임의 filesystem plugin loading, credential export, PyPI 게시를 포함하지 않습니다.
- 이번 문서 PR에서는 `release.toml`을 `draft`로 유지합니다. 전용 readiness PR만 `ready`로 변경할 수 있습니다.
- 최종 readiness 경로는 완료된 `refac` tree의 clean validation 이후, release metadata를 변경하고 정확히 검증된 계보를 승격하는 하나의 `main` 대상 readiness PR입니다.

### 문서 변경

- 영문/한글 README를 완료된 v0.3.0 기능과 남은 release-only 작업에 맞게 정리합니다.
- Release criteria에서 최종 문서 검토를 완료 처리하되 최종 validation/readiness 항목은 대기 상태로 유지합니다.
- 프로젝트 progress를 기능 개발 단계에서 readiness 준비 단계로 갱신합니다.
- 이번 PR을 bilingual engineering history에 기록합니다.
