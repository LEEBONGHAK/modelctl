# PR #2 — Stabilize lint and test collection

- Pull request: https://github.com/LEEBONGHAK/modelctl/pull/2
- Merged: 2026-08-03
- Base / head: `refac` ← `chore/ci-stabilization`
- Merge commit: `f1fe45325a69ed2846652fbbc81547a33792f80a`

## English

### Context

The first functional PR exposed pre-existing repository issues that prevented CI from serving as a useful correctness gate. The immediate goal was to restore reliable installation, lint collection, and pytest execution without starting a large style or typing refactor.

### Changes

- Made the unfinished profile service syntactically valid without expanding its behavior.
- Corrected the model-repository boolean query and formatting.
- Removed stale duplicate tests that interfered with collection.
- Ensured CI installed every uv workspace package.
- Focused Ruff on correctness rules: `E`, `F`, and `B`.
- Kept pytest as the functional regression gate.
- Deferred repository-wide basedpyright enforcement and style cleanup.

### Validation and outcome

Workspace synchronization, Ruff, and pytest completed successfully. The repository gained a stable baseline for future feature PRs while preserving the working-functionality-first development principle.

## 한국어

### 배경

첫 기능 PR을 적용하면서 기존 저장소에 남아 있던 문제들이 드러났고, CI가 신뢰할 수 있는 정확성 검증 수단으로 동작하지 못했습니다. 대규모 스타일·타입 리팩터링보다 설치, lint 수집, pytest 실행을 먼저 안정화하는 것이 목표였습니다.

### 변경 내용

- 미완성 profile service를 기능 확장 없이 문법적으로 유효하게 만들었습니다.
- 모델 저장소의 boolean 조회와 코드 형식을 수정했습니다.
- 테스트 수집을 방해하던 오래된 중복 테스트를 제거했습니다.
- CI가 uv workspace의 모든 패키지를 설치하도록 정리했습니다.
- Ruff 검사를 정확성 규칙인 `E`, `F`, `B`에 집중했습니다.
- pytest를 기능 회귀 검증 기준으로 유지했습니다.
- 저장소 전체 basedpyright 강제와 스타일 정리는 이후로 연기했습니다.

### 검증 및 결과

Workspace 동기화, Ruff, pytest가 정상 통과했습니다. 이후 기능 PR이 안정적으로 쌓일 수 있는 기준선을 마련하면서도 ‘동작하는 기능 우선’ 원칙을 유지했습니다.
