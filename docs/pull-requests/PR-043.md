# PR #43 — Declare v0.3.0 ready and promote to main

- Pull request: https://github.com/LEEBONGHAK/modelctl/pull/43
- Base branch: `main`
- Head branch: `release/v0.3.0-readiness`
- Created: 2026-09-01
- Validated `refac` baseline: `1b5639a699dc3e26e53a61ad8a9ee1dcb4933e03`
- Validated baseline tree: `aaad179a59b5f6b98e8319ff4c2b3818d84d392e`
- Status: readiness validation in progress

## English

### Context

PRs #36–#42 completed the v0.3.0 functional, plugin, diagnostics, typing, packaging, security, and documentation scope on `refac`. PR #42's final head passed CI, Package, Test, and complete Release validation with no unresolved review threads. Its Git tree is identical to merged `refac` baseline commit `1b5639a699dc3e26e53a61ad8a9ee1dcb4933e03`, which independently passed push CI, Package, and the complete Ubuntu/macOS/Windows test matrix.

This pull request adds no runtime feature. The readiness branch starts directly from that exact validated baseline and finalizes only release state and release-facing documentation.

### Readiness changes

- Change `release.toml` from `draft` to `ready` while keeping PyPI publication disabled.
- Date and finalize the v0.3.0 changelog entry.
- Mark the completed baseline validation evidence in the release criteria.
- Finalize English/Korean READMEs and project progress for the ready release state.
- Preserve profile portability as deferred evidence-driven follow-up work.

### Merge gate

The exact final readiness head must pass:

- clean locked dependency audit
- Ruff
- strict basedpyright boundary checking
- provider contract tests
- complete Python 3.13 tests on Ubuntu, macOS, and Windows
- all distribution builds
- isolated installed-wheel imports, CLI startup, `py.typed`, and installed launcher-plugin discovery
- complete release validation and checksum generation
- no unresolved review threads

After merge, the release workflow must revalidate the exact `main` merge commit before creating immutable tag `v0.3.0` and its GitHub Release. PyPI publication remains disabled.

## 한국어

### 배경

PR #36–#42에서 `refac`의 v0.3.0 기능, plugin, diagnostics, typing, packaging, security, 문서 범위를 완료했습니다. PR #42 최종 head는 미해결 review thread 없이 CI, Package, Test, 전체 Release validation을 통과했습니다. 해당 Git tree는 병합된 `refac` baseline commit `1b5639a699dc3e26e53a61ad8a9ee1dcb4933e03`과 정확히 동일하며, 이 baseline commit에서도 push CI, Package, Ubuntu/macOS/Windows 전체 테스트가 다시 통과했습니다.

이번 Pull Request는 runtime 기능을 추가하지 않습니다. Readiness branch는 정확히 검증된 baseline에서 직접 분기했고 release 상태 및 release-facing 문서만 최종화합니다.

### Readiness 변경

- PyPI 비활성화를 유지하면서 `release.toml`을 `draft`에서 `ready`로 변경합니다.
- v0.3.0 changelog 날짜와 내용을 최종화합니다.
- Release criteria에 완료된 baseline validation 근거를 기록합니다.
- 영문/한글 README와 project progress를 ready release 상태로 최종화합니다.
- Profile portability는 증거 기반 후속 작업으로 defer한 상태를 유지합니다.

### 병합 기준

정확한 최종 readiness head에서 다음 항목이 모두 통과해야 합니다.

- clean locked dependency audit
- Ruff
- strict basedpyright boundary check
- provider contract 테스트
- Python 3.13 기반 Ubuntu, macOS, Windows 전체 테스트
- 모든 배포물 build
- 격리 installed-wheel import, CLI 실행, `py.typed`, 설치 launcher-plugin discovery
- 전체 release validation과 checksum 생성
- 미해결 review thread 없음

병합 후 release workflow는 불변 tag `v0.3.0`과 GitHub Release를 생성하기 전에 정확한 `main` merge commit을 다시 검증해야 합니다. PyPI 게시는 계속 비활성화합니다.
