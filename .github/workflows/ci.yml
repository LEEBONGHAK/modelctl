name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  lint-and-audit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          python-version: "3.13"
          enable-cache: true

      - name: Install workspace
        run: uv sync --all-packages --locked

      - name: Audit locked dependencies
        # cryptography 49.0.0 is affected only in PKCS#7 EnvelopedData decryption,
        # which modelctl does not use. Remove this ignore when 50.0.0 is available.
        run: uv audit --locked --ignore GHSA-g6cj-pr64-35w5

      - name: Lint
        run: uv run ruff check .
