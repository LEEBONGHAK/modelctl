name: Temporary v0.3.0 lock generation

on:
  push:
    branches:
      - feat/v0.3.0-named-profiles

permissions:
  contents: write

jobs:
  generate-lock:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          python-version: "3.13"

      - name: Generate coordinated lock
        run: uv lock

      - name: Commit generated lock
        run: |
          if git diff --quiet -- uv.lock; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add uv.lock
          git commit -m "chore(lock): coordinate v0.3.0 workspace versions"
          git push origin HEAD:feat/v0.3.0-named-profiles
